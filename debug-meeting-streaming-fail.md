# [OPEN] Debug Session: meeting-streaming-fail

## 用户现象
- 开启本地服务后，会议流式无法运行。
- 用户已更新 API Key，但仍然无效。

## 当前阶段
- Step 3: 已完成最小运行时插桩，并通过浏览器复现获取到首轮证据。

## 初始假设
- H1: 新 API Key 没有真正写入当前会议页生效配置，页面仍在读取旧值、空值或其他存储值。
- H2: 流式识别的 WebSocket 建连失败，可能由鉴权、接口地址、模型参数或请求格式导致。
- H3: 麦克风采集未正常启动，导致没有音频数据进入流式链路。
- H4: 当前分支/本地服务下存在资源或模块初始化问题，导致流式链路在启动前就失败。

## 下一步
- 继续确认用户为何认为“已连上但没字”，但运行时实际读到的 `apiKey` 为空。
- 只做证据判断，不修改业务逻辑。

## 当前证据
- 本地真实入口为 `http://localhost:8000/app/index.html`。
- 页面已加载当前脚本：`meeting-page-app.js?v=20260630-merged`，不是旧缓存脚本。
- 浏览器自动化点击“开始会议”后，页面没有进入 WebSocket 建连阶段。
- 运行时在 `startMeeting()` 的 API Key 判空分支直接返回。
- 页面状态表现为：
  - `configStatus`: `请先输入 SenseAudio API Key。`
  - `recordingStatusText`: `缺少 Key`
  - `transcriptStatusText`: `缺少 Key`
- 会议页中的 `#apiKeyInput` 在复现时长度为 `0`。

## 假设状态
- H1: **暂时确认**。当前会议页实际拿到的 API Key 为空，说明新 Key 没有真正进入会议页当前生效配置。
- H2: **暂未支持**。本轮证据尚未进入 WebSocket 建连阶段，因此不能判定是鉴权或接口问题。
- H3: **暂未支持**。本轮证据尚未进入音频采集阶段，因此不能判定是麦克风或送音问题。
- H4: **基本排除**。当前页面已加载最新会议脚本，不像是浏览器仍在跑旧缓存页面。
